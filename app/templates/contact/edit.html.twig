{% extends 'base.html.twig' %}

{% block title %}Edit {{ contact.name }} {{ contact.surname }}{% endblock %}

{% block body %}
    <h1>Edit {{ contact.name }} {{ contact.surname }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_home') }}">Back to list</a>
{% endblock %}
